function [ Featurenorm ] = Normaliztion_Shaibal_Features( Feature )
% Normaliztion of 132*1 Features table
start=1;
stop=132;
% for i = 1:33
    Featuretemp = Feature(start:stop);
    
    tempsum = sum(Featuretemp);
%     for j =1:4;
        normtemp = 132*(Featuretemp/tempsum);
%     end;
    
    Featurenorm(start:stop) = normtemp;
    Featurenorm = Featurenorm';
%     start=start+4;
%     stop=stop+4;
%     

% 0-1 range normalization with 0.1 offset

normalization = (Featuretemp- min(Featuretemp))/(max(Featuretemp)-min(Featuretemp))+0.1


end

